import Link from "next/link";import {cn} from "@/lib/utils";import type {SkillItem} from "@/types";
const levelConfig={learning:{label:"Learning",dot:"bg-signal"},practiced:{label:"Practiced",dot:"bg-cobalt"},comfortable:{label:"Comfortable",dot:"bg-teal"}};
export function SkillBadge({skill}:{skill:SkillItem}){const c=levelConfig[skill.level];const has=!!skill.relatedProjectSlugs?.length;return <div className="flex items-center justify-between gap-3 border border-border bg-surface-2 px-3.5 py-3"><div className="flex min-w-0 items-center gap-2.5"><span className={cn("h-1.5 w-1.5 shrink-0 rounded-full",c.dot)}/><span className="truncate text-sm text-text">{skill.name}</span></div><div className="flex shrink-0 items-center gap-2"><span className="label">{c.label}</span>{has&&<Link href={`/projects/${skill.relatedProjectSlugs![0]}`} className="label border border-border px-1.5 py-0.5 text-cobalt hover:border-border-strong">in project</Link>}</div></div>}
