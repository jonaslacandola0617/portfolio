import type {Metadata} from "next";import {PageHeader,PageShell} from "@/components/shared/page-header";import {SkillBadge} from "@/components/shared/skill-badge";import {Icon} from "@/components/shared/icon-map";import {getAllSkillCategories} from "@/lib/db/queries/skills";
export const metadata:Metadata={title:"Skills"};
export default async function SkillsPage(){const cats=await getAllSkillCategories();return <div><PageHeader index="07" eyebrow="Capability Matrix" title="Skills" description="Grouped capabilities with honest learning states instead of percentage bars. Linked skills point to where they were actually used."/><PageShell>{cats.length?<div className="space-y-10">{cats.map((cat,i)=><section key={cat.category}><div className="mb-4 flex items-center gap-3"><span className="idx">{String(i+1).padStart(2,"0")}</span><Icon name={cat.icon} className="h-4 w-4 text-cobalt"/><h2 className="font-display text-xl font-semibold text-text">{cat.category||"Ungrouped"}</h2><span className="rule ml-2 flex-1"/></div><div className="grid gap-2 sm:grid-cols-2">{cat.skills.map(s=><SkillBadge key={s.name} skill={s}/>)}</div></section>)}</div>:<p className="text-sm text-text-dim">No published skills yet.</p>}<div className="mt-12 flex flex-wrap gap-4 border-t border-border pt-6 label"><span className="flex items-center gap-1.5"><span className="h-1.5 w-1.5 rounded-full bg-signal"/>Learning</span><span className="flex items-center gap-1.5"><span className="h-1.5 w-1.5 rounded-full bg-cobalt"/>Practiced</span><span className="flex items-center gap-1.5"><span className="h-1.5 w-1.5 rounded-full bg-teal"/>Comfortable</span></div></PageShell></div>}
