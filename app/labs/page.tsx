import type {Metadata} from "next";import {PageHeader,PageShell} from "@/components/shared/page-header";import {LabCard} from "@/components/shared/lab-card";import {getAllLabs} from "@/lib/content";
export const metadata:Metadata={title:"Labs"};
export default async function LabsPage(){const labs=await getAllLabs();return <div><PageHeader index="03" eyebrow="Lab Notebook" title="Labs" description="A chronological log of hands-on labs — purpose, commands, what went wrong, and what fixed it. Logged close to when the work happened, mistakes included."/><PageShell>{labs.length?<div className="space-y-5">{labs.map((l,i)=><LabCard key={l.frontmatter.slug} lab={l.frontmatter} index={i+1}/>)}</div>:<p className="text-sm text-text-dim">No published labs yet.</p>}</PageShell></div>}
